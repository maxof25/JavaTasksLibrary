public class AboutMe {
	public static void main(String[] args) {
		String name = "Max Field";
		//I first declare the variable of which will be holding a value that is a string.
		//I then place the value i wish to store in the variable in name. The value is "Max Field".
		char gender = 'M';
		//I then declare the variable of which will be holding a value that is a char (a single character).
		//In the variable gender, the char value 'M' is held.
		int age = 16;
		//I then declare the variable of which will be holding a value that is an integer (a whole number).
		//In the variable age, the int value '16' is held.
		double height = 1.9;
		//I then declare the variable of which will be holding a value that is a double (not a whole number(integer)).
		//In the variable height, the double value '1.9' is held.
		boolean married = false;
		//I then declare the variable of which will be holding a value that is a boolean (true or false values).
		//In the variable married, the boolean value 'false' is held.
		System.out.println("My name is: " + name);
		//This line will print out "My name is: " then the stored value in the variable name in the terminal of the IDE.
		System.out.println("My gender is: " + gender);
		//This line will print out "My gender is: " then the stored value in the variable gender in the terminal of the IDE.
		System.out.println("My age is: " + age);
		//This line will print out "My age is: " followed by the stored value in the variable age in the terminal of the IDE.
		System.out.println("My height is: " + height);
		//This line will print out "My height is: " followed by the stored value in the variable age in the terminal of the IDE.
		System.out.println("Am I married? " + married);
		//This line will ask the question "Am I married? " followed by the stored value in the variable married in the terminal of the IDE.
	}
}
